import app from '../app';

describe('server/app.js', function() {
    it('dummy test to get coverage of app', function() {
        // never intend to test the app.js file
        expect(1).to.equal(1);
    });
});
